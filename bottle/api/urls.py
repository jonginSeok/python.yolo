# api/urls.py

from django.urls import path
from . import views  # 또는 from api import views

urlpatterns = [
    # path('api/', views.api_view, name="api"),  # 예시
    # path('api/some-endpoint/', views.some_view, name="some_view"),
    # 여기에 API 관련 URL 패턴을 추가하세요
]
